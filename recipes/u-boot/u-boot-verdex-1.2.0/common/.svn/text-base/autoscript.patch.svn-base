Index: u-boot-1.2.0/common/cmd_autoscript.c
===================================================================
--- u-boot-1.2.0.orig/common/cmd_autoscript.c
+++ u-boot-1.2.0/common/cmd_autoscript.c
@@ -110,7 +110,7 @@ autoscript (ulong addr)
 		return 1;
 	}
 
-	while (*len_ptr++);
+	do { len_ptr++; } while (!(*len_ptr));
 
 	/* make sure cmd is null terminated */
 	memmove (cmd, (char *)len_ptr, len);
